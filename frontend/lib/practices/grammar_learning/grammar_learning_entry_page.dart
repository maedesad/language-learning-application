import 'package:flutter/material.dart';

class GrammarLearningEntryPage extends StatelessWidget {
  const GrammarLearningEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Grammar Learning Entry Page"),
    );
  }
}