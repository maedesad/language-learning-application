import 'package:flutter/material.dart';

class ConversationEntryPage extends StatelessWidget {
  const ConversationEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Conversation Entry Page"),
    );
  }
}