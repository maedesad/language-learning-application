import 'package:flutter/material.dart';

class GrammarReviewEntryPage extends StatelessWidget {
  const GrammarReviewEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Grammar Review Entry Page"),
    );
  }
}