import 'package:flutter/material.dart';

class VocabGrammarReviewEntryPage extends StatelessWidget {
  const VocabGrammarReviewEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Vocab & Grammar Review Entry Page"),
    );
  }
}