import 'package:flutter/material.dart';

class ReadingEntryPage extends StatelessWidget {
  const ReadingEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Reading Entry Page"),
    );
  }
}