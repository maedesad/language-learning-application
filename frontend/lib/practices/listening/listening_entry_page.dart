import 'package:flutter/material.dart';

class ListeningEntryPage extends StatelessWidget {
  const ListeningEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Listening Entry Page"),
    );
  }
}