import 'package:flutter/material.dart';

class VocabReviewEntryPage extends StatelessWidget {
  const VocabReviewEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Vocab Review Entry Page"),
    );
  }
}