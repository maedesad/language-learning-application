import 'package:flutter/material.dart';

class WritingEntryPage extends StatelessWidget {
  const WritingEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Writing Entry Page"),
    );
  }
}